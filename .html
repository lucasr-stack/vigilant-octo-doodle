<!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Frontend Developer</title>
  <style>
    :root {
      --bg: #eef4ff;
      --surface: #ffffff;
      --border: #213a72;
      --text: #102147;
      --muted: #5d6b8b;
      --accent: #1b60d1;
      --accent-soft: #d9e4ff;
      --radius: 18px;
      --shadow: 0 18px 40px rgba(33, 58, 114, 0.12);
      --max-width: 1080px;
    }

    * {
      box-sizing: border-box;
    }

    html {
      scroll-behavior: smooth;
    }

    body {
      margin: 0;
      font-family: Inter, system-ui, sans-serif;
      background: linear-gradient(180deg, #f2f7ff 0%, #eef4ff 100%);
      color: var(--text);
      display: flex;
      justify-content: center;
      padding: 24px;
    }

    .container {
      width: min(100%, var(--max-width));
      background: var(--surface);
      border: 1px solid rgba(33, 58, 114, 0.17);
      border-radius: var(--radius);
      box-shadow: var(--shadow);
      padding: 32px;
    }

    header {
      display: flex;
      align-items: center;
      justify-content: space-between;
      gap: 24px;
      border-bottom: 1px solid rgba(33, 58, 114, 0.18);
      padding-bottom: 18px;
      margin-bottom: 32px;
    }

    .logo {
      display: flex;
      align-items: center;
      gap: 12px;
      font-weight: 700;
      text-transform: lowercase;
      letter-spacing: -0.02em;
      color: var(--text);
    }

    .logo-box {
      width: 42px;
      height: 42px;
      background-color: var(--accent);
      border-radius: 12px;
    }

    .nav-wrap {
      display: flex;
      align-items: center;
      gap: 18px;
    }

    .nav-toggle {
      display: none;
      flex-direction: column;
      gap: 5px;
      cursor: pointer;
    }

    .nav-toggle span {
      width: 24px;
      height: 3px;
      background: var(--text);
      border-radius: 999px;
    }

    #nav-toggle {
      display: none;
    }

    .main-nav {
      display: flex;
      gap: 20px;
      align-items: center;
      flex-wrap: wrap;
    }

    .main-nav a {
      color: var(--text);
      text-decoration: none;
      font-size: 0.95rem;
      transition: color 0.2s ease;
    }

    .main-nav a:hover,
    .main-nav a:focus-visible {
      color: var(--accent);
      outline: none;
    }

    .hero {
      display: grid;
      place-items: center;
      text-align: center;
      padding: 40px 0;
      border-bottom: 1px solid rgba(33, 58, 114, 0.18);
      margin-bottom: 32px;
    }

    .hero h1 {
      margin: 0;
      font-size: clamp(2.4rem, 4vw, 3.6rem);
      letter-spacing: -0.04em;
    }

    .hero p {
      margin: 18px 0 0;
      color: var(--muted);
      font-size: 1rem;
      max-width: 620px;
    }

    .grid {
      display: grid;
      grid-template-columns: 1fr 1.1fr 1.1fr;
      gap: 20px;
      margin-bottom: 32px;
    }

    .card {
      background: var(--accent-soft);
      border: 1px solid rgba(33, 58, 114, 0.15);
      border-radius: 20px;
      padding: 24px;
      box-shadow: inset 0 0 0 1px rgba(255,255,255,0.65);
    }

    .card h2 {
      margin-top: 0;
      font-size: 1rem;
      text-transform: uppercase;
      letter-spacing: 0.18em;
      color: var(--accent);
    }

    .card ul,
    .card p,
    .card ol {
      margin: 16px 0 0;
      line-height: 1.72;
      color: var(--text);
    }

    .card ul {
      padding-left: 18px;
    }

    .card ol {
      padding-left: 18px;
    }

    .work a {
      color: var(--accent);
      text-decoration: none;
      font-size: 0.95rem;
    }

    .work a:hover {
      text-decoration: underline;
    }

    .reviews-section,
    .contact-section {
      border-top: 1px solid rgba(33, 58, 114, 0.18);
      padding-top: 32px;
      margin-bottom: 24px;
    }

    .reviews-title,
    .contact-section h2 {
      margin: 0 0 20px;
      font-size: 1.15rem;
      font-weight: 700;
      letter-spacing: 0.06em;
      color: var(--text);
    }

    .reviews-grid {
      display: grid;
      grid-template-columns: repeat(3, 1fr);
      gap: 20px;
    }

    .review-card {
      border: 1px solid rgba(33, 58, 114, 0.15);
      border-radius: 20px;
      padding: 24px;
      background: #fff;
      min-height: 180px;
    }

    .review-card p {
      margin: 0 0 14px;
      font-size: 0.95rem;
      color: var(--text);
    }

    .review-meta {
      font-size: 0.87rem;
      color: var(--muted);
      line-height: 1.5;
    }

    .contact-card {
      background: var(--surface);
    }

    .contact-card p {
      margin-bottom: 20px;
      color: var(--muted);
      max-width: 640px;
    }

    .contact-form {
      display: grid;
      gap: 16px;
    }

    .form-group {
      display: grid;
      gap: 10px;
    }

    .form-group label {
      font-size: 0.95rem;
      color: var(--text);
      font-weight: 600;
    }

    .form-group input,
    .form-group textarea {
      width: 100%;
      padding: 14px 16px;
      border: 1px solid rgba(33, 58, 114, 0.18);
      border-radius: 14px;
      background: #f7f9ff;
      font: inherit;
      resize: vertical;
      color: var(--text);
    }

    .form-group input:focus,
    .form-group textarea:focus {
      outline: 2px solid rgba(27, 96, 209, 0.25);
      border-color: var(--accent);
      background: #fff;
    }

    .btn {
      width: fit-content;
      margin-top: 8px;
      padding: 14px 26px;
      border: none;
      border-radius: 14px;
      background: var(--accent);
      color: #fff;
      font-weight: 700;
      cursor: pointer;
      transition: transform 0.2s ease, background-color 0.2s ease;
    }

    .btn:hover {
      transform: translateY(-1px);
      background: #174bb2;
    }

    footer {
      margin-top: 16px;
      text-align: center;
      font-size: 0.95rem;
      color: var(--muted);
    }

    @media (max-width: 860px) {
      .grid,
      .reviews-grid {
        grid-template-columns: 1fr;
      }

      header {
        flex-direction: column;
        align-items: stretch;
      }

      .nav-wrap {
        width: 100%;
        justify-content: space-between;
      }

      .main-nav {
        width: 100%;
        justify-content: flex-start;
        flex-wrap: wrap;
        gap: 14px;
      }
    }

    @media (max-width: 680px) {
      body {
        padding: 16px;
      }

      .container {
        padding: 24px;
      }

      .nav-toggle {
        display: flex;
      }

      .main-nav {
        display: none;
        flex-direction: column;
        gap: 14px;
        padding-top: 14px;
        width: 100%;
      }

      #nav-toggle:checked + .main-nav {
        display: flex;
      }

      .main-nav a {
        display: block;
        width: 100%;
      }

      .hero {
        padding: 30px 0;
      }

      .reviews-grid {
        grid-template-columns: 1fr;
      }
    }
  </style>
</head>
<body>
  <div class="container">
    <header>
      <div class="logo">
        <div class="logo-box"></div>
        <span>your name</span>
      </div>
      <div class="nav-wrap">
        <label for="nav-toggle" class="nav-toggle" aria-label="Open menu">
          <span></span>
          <span></span>
          <span></span>
        </label>
        <input type="checkbox" id="nav-toggle">
        <nav class="main-nav">
          <a href="#home">Home</a>
          <a href="#projects">Projects</a>
          <a href="#articles">Articles</a>
          <a href="#contact">Contact</a>
        </nav>
      </div>
    </header>

    <section class="hero" id="home">
      <h1>Frontend Developer</h1>
      <p>html only with proper layout, no styling</p>
    </section>

    <section class="grid">
      <article class="card" id="projects">
        <h2>Projects</h2>
        <ul>
          <li>HTML Only Portfolio</li>
          <li>Calculator</li>
          <li>Quiz App</li>
          <li>Countdown Timer</li>
          <li>Product Upcoming Page</li>
        </ul>
      </article>

      <article class="card work">
        <h2>Work Experience</h2>
        <p><strong>roadmap.sh</strong><br>
          Solved all the frontend projects.<br>
          <a href="https://roadmap.sh/u/lucastack" target="_blank" rel="noreferrer">Visit my Profile</a>
        </p>
        <p><strong>OpenSource Work</strong><br>
          Contributed to 50 opensource projects. Made my own projects with 200 GitHub Stars.<br>
          <a href="https://github.com/lucasr-stack/lucasr-stack.github.io/" target="_blank" rel="noreferrer">Visit my GitHub Profile</a>
        </p>
      </article>

      <article class="card">
        <h2>Education</h2>
        <p>Graduated with 3.76 out of 4 CGPA. Won Acme Hackathon. Organized 30 sessions.</p>
        <ol>
          <li>Object Oriented Programming</li>
          <li>Data Structures and Algorithms</li>
          <li>Web Engineering</li>
          <li>Artificial Intelligence</li>
          <li>Human Computer Interaction</li>
          <li>Computer Graphics</li>
          <li>Database Management Systems</li>
          <li>Distributed Database Systems</li>
          <li>Discrete Mathematics</li>
        </ol>
      </article>
    </section>

    <section class="reviews-section">
      <h2 class="reviews-title">Reviews from my Teachers</h2>
      <div class="reviews-grid">
        <div class="review-card">
          <p>John doe was a brilliant student; always stood out with his assignments.</p>
          <div class="review-meta">Jane Doe<br>Assistant Professor</div>
        </div>
        <div class="review-card">
          <p>John doe was a brilliant student; always stood out with his assignments.</p>
          <div class="review-meta">Jane Doe<br>Assistant Professor</div>
        </div>
        <div class="review-card">
          <p>John doe was a brilliant student; always stood out with his assignments.</p>
          <div class="review-meta">Jane Doe<br>Assistant Professor</div>
        </div>
      </div>
    </section>

    <section class="contact-section" id="contact">
      <div class="contact-card card">
        <h2>Contact</h2>
        <p>Have a frontend project in mind? Send a quick note and I will get back to you.</p>
        <form action="#" method="post" class="contact-form">
          <div class="form-group">
            <label for="name">Name</label>
            <input id="name" name="name" type="text" placeholder="Your name" required>
          </div>
          <div class="form-group">
            <label for="email">Email</label>
            <input id="email" name="email" type="email" placeholder="you@example.com" required>
          </div>
          <div class="form-group">
            <label for="message">Message</label>
            <textarea id="message" name="message" rows="5" placeholder="Tell me about your project"></textarea>
          </div>
          <button type="submit" class="btn">Send message</button>
        </form>
      </div>
    </section>

    <footer>
      © all rights reserved 2025
    </footer>
  </div>
</body>
</html>
